version: '2'

services:
  db:
    image: postgres
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ".:/src"
    ports:
      - "8080:8080"
    depends_on:
     - db